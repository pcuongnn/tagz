  <% if @order.line_items.empty? %>
    
<a name="about"></a>
  
        <div class="container">

            <div class="row rfloat">
                <div class="col-lg-12 cart021">
                    <div class="intro-message">
                        
        
        
        <div class="col-md-12 talign">
            <h3 class="abt icmargin2"> Cart</h3>
     
            <h4 class="hcol"> Common you need to have something in bag !</h4></div>
        <div class="col-md-12 cart022">
         <%= link_to "SHOP", products_path, class: "btn btn-default btn-lg btnonh submit dupsub subbtn tmargin vvt", :style => "color:#FFFFFF" %>
        </div>
                    </div>
                </div>
            </div>

        </div>

  <% else %>
    <div data-hook="outside_cart_form">
      <%= form_for @order, url: update_cart_path, html: { id: 'update-cart' } do |order_form| %>
        <div data-hook="inside_cart_form">

          <div data-hook="cart_items" class="table-responsive">
            <%= render partial: 'form', locals: { order_form: order_form } %>
          </div>

          <!-- <div class="links col-md-6 navbar-form pull-right text-right" data-hook="cart_buttons">
            <div class="form-group">
              <%= button_tag class: 'btn btn-primary', id: 'update-button' do %>
                <%= Spree.t(:update) %>
              <% end %>
              <%= button_tag class: 'btn btn-lg btn-success', id: 'checkout-link', name: 'checkout' do %>
                <%= Spree.t(:checkout) %>
              <% end %>
            </div>
          </div> -->
        </div>
      <% end %>
    </div>

    <!-- <div id="empty-cart" class="col-md-6 pull-left" data-hook>
      <%= form_tag empty_cart_path, method: :put do %>
        <p id="clear_cart_link" data-hook>
          <%= submit_tag Spree.t(:empty_cart), class: 'btn btn-default' %>
          <%= Spree.t(:or) %>
          <%= link_to Spree.t(:continue_shopping), products_path, class: 'continue' %>
        </p>
      <% end %>
    </div> -->
  <% end %>
</div>
<footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 ">
                    <ul class="list-inline footer43">
                        <li>
                            <a href="#">Terms and Conditions</a>
                        </li>
                        <li class="footer-menu-divider">|</li>
                        <li>
                            <a href="#about">FAQ</a>
                        </li>
                        <li class="footer-menu-divider">|</li>
                        <li>
                            <a href="#services">Contact</a>
                        </li>
                        
                    </ul>
                  
                </div>
            </div>
        </div>
    </footer>